{
  "name": "inventsoft",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix nextjs",
    "dev":         "npm run dev --prefix nextjs",
    "build":       "npm run build --prefix nextjs",
    "start":       "npm run start --prefix nextjs",
    "db:push":     "npm run db:push --prefix nextjs",
    "db:generate": "npm run db:generate --prefix nextjs",
    "db:migrate":  "npm run db:migrate --prefix nextjs",
    "db:studio":   "npm run db:studio --prefix nextjs",
    "db:seed":     "npm run db:seed --prefix nextjs"
  }
}
